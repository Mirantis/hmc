apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "hmc.fullname" . }}-multiclusterservices-viewer-role
rules:
  - apiGroups:
      - hmc.mirantis.com
    resources:
      - multiclusterservices
      - servicetemplates
    verbs: {{ include "rbac.viewerVerbs" . | nindent 6 }}
