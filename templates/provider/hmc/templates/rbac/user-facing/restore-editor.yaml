# permissions for end users to edit restores.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    hmc.mirantis.com/aggregate-to-global-admin: "true"
  name: {{ include "hmc.fullname" . }}-restore-editor-role
rules:
- apiGroups:
  - hmc.mirantis.com
  resources:
  - restores
  - restores/status
  verbs: {{ include "rbac.editorVerbs" . | nindent 6 }}
