# permissions for end users to view restores.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    hmc.mirantis.com/aggregate-to-global-viewer: "true"
  name: {{ include "hmc.fullname" . }}-restore-viewer-role
rules:
- apiGroups:
  - hmc.mirantis.com
  resources:
  - restores
  - restores/status
  verbs: {{ include "rbac.viewerVerbs" . | nindent 6 }}
